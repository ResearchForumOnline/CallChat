# Bot Command Draft

Public user commands:

- `!zero help`
- `!zero setup`
- `!zero shield`
- `!zero calls`
- `!zero apps`
- `!zero privacy`
- `!zero forget-room`

Admin-only commands:

- `!zero status`
- `!zero approved-rooms`
- `!zero rate-limit`
- `!zero server-summary`

Voice commands, if enabled:

- `!zero voice <text>`

Voice features should be rate limited and clearly marked as generated audio.
